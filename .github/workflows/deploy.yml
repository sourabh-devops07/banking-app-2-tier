name: Deploy Application on Environment

on:
  workflow_dispatch

jobs:
  working-job:
    runs-on: ubuntu-latest

    steps:
      - name: Echo Message
        run: echo "Application Deployed On Environment successfully ✅"